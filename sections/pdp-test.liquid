{% assign test_pdp = product.metafields.custom.works_well_with.values %}
{{ product.metafields.custom.works_well_with }}
{{ product.metafields.custom.works_well_with.value | json }}
{% for pdp in product.metafields.custom.works_well_with %}
  {{ pdp }}
{% endfor %} 
{% schema %}
{
  "name": "PDP Test",
  "settings": [

   
    {
      "type": "product_list",
      "id": "product_list",
      "label": "Product list"
    }
  ],
  "presets": [
    {
      "name": "PDP Test",
      "category": "Collection"
    }
  ]
}
  
{% endschema %}