{% assign test_pdp = product.metafields.custom.works_well_with %}  
{{ section.settings.product_list | json }}
{{ product.metafields.custom.works_well_with }}
{% for pdp in test_pdp %}
  {{ pdp }} => 100
{% endfor %} 
{% schema %}
{
  "name": "PDP Test",
  "settings": [

   
    {
      "type": "product_list",
      "id": "product_list",
      "label": "Product list"
    }
  ],
  "presets": [
    {
      "name": "PDP Test",
      "category": "Collection"
    }
  ]
}
  
{% endschema %}