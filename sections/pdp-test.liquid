{% assign product_list = section.settings.product_list %}  
{{ section.settings.product_list | json }}
{{ product | json}}
{% for pdp in product_list %}
  {{ pdp }} => 100
{% endfor %} 
{% schema %}
{
  "name": "PDP Test",
  "settings": [

   
    {
      "type": "product_list",
      "id": "product_list",
      "label": "Product list"
    }
  ],
  "presets": [
    {
      "name": "PDP Test",
      "category": "Collection"
    }
  ]
}
  
{% endschema %}